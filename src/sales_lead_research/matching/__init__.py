# agent-notes: { ctx: "matching subpackage: name normalisation today, customer-DB lookup later", deps: [], state: active, last: "sato@2026-04-28" }
"""Customer-matching subpackage.

Public surface is intentionally empty for Wave 2.1 — callers import
``sales_lead_research.matching.names`` directly. The exported API will
be settled in Wave 3.x once ``store.py`` lands.
"""
