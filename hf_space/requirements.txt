# The web UI now lives in the package, so the Space just installs it
# (this pulls gradio, httpx, beautifulsoup4, pypdf, rich). Requires this
# repo to be public (or add an access token to the URL). Pin to a tag or
# commit for reproducible Space builds.
sales-lead-research @ git+https://github.com/mahadevaiahrashmi/sales_lead_research.git@main
